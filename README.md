# web-scrapping-flaskApi

* python -m venv venv
* source venv/Scripts/activate
* pip install Flask
* pip install flask-restful or pip install Flask-RESTful
* pip install requests
* pip install beautifulsoup4
